[package]
name = "py-venv"
version = "0.1.0"
edition = "2021"

[dependencies]
pyo3 = { version = "0.28", features = ["auto-initialize"] }

[dev-dependencies]
pyo3 = { version = "0.28", features = ["auto-initialize"] }
