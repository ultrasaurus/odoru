{
  "name": "player-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:wasm": "wasm-pack build ../../splitter-wasm --target web --out-dir ../app/frontend/src/wasm",
    "predev": "npm run build:wasm",
    "prebuild": "npm run build:wasm",
    "pretest": "npm run build:wasm",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.4.0",
    "vite": "^8.0.14",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "marked": "^18.0.4"
  },
  "allowScripts": {
    "fsevents@2.3.3": true
  }
}
